<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.palette.h5.ego.dao.EgoMapper">

	<!-- Swot분석 입력 -->
	
	<!-- HISTORY | history 전체 데이터 가져오기 -->
	<select id="historyDataAll" parameterType="string" resultType="history">
		select 
			hisId
			,hisDate
			,hisPeriod
			,hisTitle
			,hisContent
			,hisNo
		from 
			history
		where
			hisId = #{hisId} 
	</select>
	
	<!-- HISTORY | history 전체 연도list 가져오기 -->
	<select id="historyYearList" parameterType="string" resultType="string">
		select
			hisDate
		from
			history
		where hisId = #{hisId}
	</select>
	
	<!-- HISTORY | history 등록하기 -->
	<insert id="historyAddOne" parameterType="history">
		insert into
			history(
				hisId
				,hisDate
				,hisPeriod
				,hisTitle
				,hisContent
				,hisNo
			) values (
			
			)
	</insert>
	
	<!-- HISTORY | history 수정하기 -->
	
	<!-- HISTORY | history 삭제하기 -->
	
	


	<!-- 파일 저장 -->
	<insert id="file_management" parameterType="file">
		insert into file_management (
			file_id
			<if test="originalFileName != null and savedFileName != null">
			, originalFileName
			, savedFileName
			</if>
		)
		values (
			file_management_SEQ.nextval
			<if test="originalFileName != null and savedFileName != null">
			, #{originalFileName}
			, #{savedFileName}
			</if>
		)
	</insert>

	<!-- 프로젝트 리스트 입력 -->
	<insert id="projectWrite_One" parameterType="CertProject" >
	
		insert into cert_project(
			proNum
			, proId
			, proTerm
			, proEdu
			, proTitle
		)
		values(
			cert_project_SEQ.nextval
			, #{proId}
			, #{proTerm}
			, #{proEdu}
			, #{proTitle}
			)
	
	</insert>
	
	<!-- 프로젝트 리스트 출력 -->
	<select id="projectList" parameterType="String" resultType="CertProject">
	select
	* 
	from 
	cert_project 
	where 
	proId = #{proId}
	</select>
	
 	<!-- 프로젝트 디테일 글 하나 출력 -->
 	<select id="projectdetail_One" parameterType="int" resultType="CertProject">
 	select
	* 
	from 
	cert_project 
	where 
	proNum = #{proNum}
 	</select>
 	
 	<!-- 프로젝트 디테일 글 수정 (입력)-->
 	<insert id="projecDetailtWrite" parameterType="CertProjectDetail">
 	
 	insert into cert_project_detail(
			proDetailNum
			, proDetailTitle
			, proDetailDate
			, proDetailEdu
			, proDetailContent
			<if test="proDetailOriginalfile != null and proDetailSavedfile != null">
			,proDetailOriginalfile
			,proDetailSavedfile
			</if>
		)
		values(
			#{proDetailNum}
			, #{proDetailTitle}
			, #{proDetailDate}
			, #{proDetailEdu}
			, #{proDetailContent}
			<if test="proDetailOriginalfile != null and proDetailSavedfile != null">
			,#{proDetailOriginalfile}
			,#{proDetailSavedfile}
			</if>
			)
 	</insert>
 	
 	<!-- 프로젝트 디테일 글 수정 (입력) 출력-->
 	<select id="projecDetail_one_one" parameterType="int" resultType="CertProjectDetail">
 	select
 	*
 	from
 	cert_project_detail
 	where
 	proDetailNum = #{proDetailNum}
 	</select>
</mapper>
